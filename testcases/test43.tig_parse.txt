   letExp(
    decList(
     varDec(a,
      nilExp(),
      TRUE),
     decList()),
    seqExp(
     expList(
      opExp(
       ,
       varExp(
        simpleVar(a)),
       intExp(3)),
      expList())))