   letExp(
    decList(
     functionDec(
      fundecList(
       fundec(nfactor,
        fieldList(
         field(n,
          int,
          TRUE),
         fieldList()),
        int,
        iffExp(
         opExp(
          ,
          varExp(
           simpleVar(n)),
          intExp(0)),
         intExp(1),
         opExp(
          ,
          varExp(
           simpleVar(n)),
          callExp(nfactor,
           expList(
            opExp(
             ,
             varExp(
              simpleVar(n)),
             intExp(1)),
            expList()))))),
       fundecList())),
     decList()),
    seqExp(
     expList(
      callExp(nfactor,
       expList(
        intExp(10),
        expList())),
      expList())))