   letExp(
    decList(
     varDec(rec1,
      recordExp(rectype1,
       efieldList(
        efield(name,
         stringExp("Kapoios")),
        efieldList(
         efield(address,
          stringExp("Kapou")),
         efieldList(
          efield(id,
           intExp(2432)),
          efieldList(
           efield(age,
            intExp(44)),
           efieldList()))))),
      TRUE),
     decList()),
    seqExp(
     expList(
      intExp(1),
      expList())))