   letExp(
    decList(
     typeDec(
      nametyList(
       namety(rectype,
        recordTy(
         fieldList(
          field(name,
           string,
           TRUE),
          fieldList(
           field(id,
            int,
            TRUE),
           fieldList())))),
       nametyList())),
     decList(
      varDec(b,
       rectype,
       nilExp(),
       TRUE),
      decList())),
    seqExp(
     expList(
      opExp(
       ,
       varExp(
        simpleVar(b)),
       nilExp()),
      expList(
       opExp(
        ,
        varExp(
         simpleVar(b)),
        nilExp()),
       expList()))))